Creating ML models from scratch without AI code agents 

Prohibited to use any machine learning libraries for any reasons except comparing their results with local ones
What is permitted to use:
    numpy
    pandas (or any library for working with datasets)
    matplotlib (or any library for vizualisation)

Metrics also should be implemented from scratch with numpy only