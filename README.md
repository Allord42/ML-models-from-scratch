## Creating ML models from scratch without AI code agents

**Prohibited:**
- Using any machine learning libraries for any reason  
  (except comparing their results with local implementations)

**Permitted:**
- `numpy`  
- `pandas` (or any library for working with datasets)  
- `matplotlib` (or any library for visualization)

**Metrics:**
- Must be implemented from scratch using **numpy only**
